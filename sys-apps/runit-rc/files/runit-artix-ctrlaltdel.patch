--- a/script/ctrlaltdel.in
+++ b/script/ctrlaltdel.in
@@ -3,8 +3,12 @@
 PATH=/usr/bin:/usr/sbin
 MSG="System is going down..."
 
+. @RCDIR@/rc.conf
+
 # We check for this file in stage3 to halt or reboot
-touch @RUNDIR@/reboot
+if [ -n "$REBOOT" ]; then
+    touch @RUNDIR@/reboot
+fi
 
 # Proceed with shutdown process
 echo "$MSG" | wall
