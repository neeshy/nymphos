--- a/openssh/files/sshd/run
+++ b/openssh/files/sshd/run
@@ -1,4 +1,4 @@
 #!/bin/sh
 ssh-keygen -A >/dev/null 2>&1 # Will generate host keys if they don't already exist
 [ -r conf ] && . ./conf
-exec /usr/bin/sshd -D $OPTS
+exec /usr/sbin/sshd -D $OPTS
